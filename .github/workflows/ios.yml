name: iOS

on:
  pull_request:
    paths:
      - 'apps/ios/**'
      - 'Package.swift'
      - '.github/workflows/ios.yml'
  push:
    branches: [ main ]
    paths:
      - 'apps/ios/**'
      - 'Package.swift'
      - '.github/workflows/ios.yml'

jobs:
  build:
    runs-on: macos-15
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install xcodegen
        run: brew install xcodegen

      - name: Generate project
        run: xcodegen generate --spec apps/ios/project.yml --project apps/ios/Shitter.xcodeproj

      - name: Download ios_system frameworks
        run: ./apps/ios/scripts/download-ios-system.sh

      - name: Build (ShitterRemote)
        run: |
          xcodebuild \
            -project apps/ios/Shitter.xcodeproj \
            -scheme ShitterRemote \
            -configuration Debug \
            -destination 'generic/platform=iOS Simulator' \
            build
